Rules:
- Choose internal links ONLY from the user-provided sitemap rows.
- Always include:
  1) One CTA link aligned to the funnel stage (service/quote/contact page if relevant).
  2) Two to four contextual links (definitions, supporting guides, adjacent topics).
- Do not invent URLs.
- If multiple candidates exist:
  - Prefer the most topically aligned page.
  - Prefer pages with strong matching intent.
  - Avoid sending users away from the page too early (place CTA later in the article; place contextual links where they clarify).

Output format:
- Link label: [Title] — [URL]
- Suggested anchor: (natural phrase)
- Placement note: (section where it fits)
